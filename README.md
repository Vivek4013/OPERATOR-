# OPERATOR
          AND OPERATOR 
#include <stdio.h>
int main ()
  {
    int a=12,b=25;
    printf("output = %d", a&b);
    return 0;
    }
            OR OPERATOR 
    #include <stdio.h>
    int main()
      {
      int a=12,b=25;
      printf("output =%d,a!b");
      return 0;
      }
      
      
      
